name: Research Yahoo adjusted market pilot 2013-2022

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/research-market-yahoo-pilot.yml"
      - "scripts/research_market_yahoo_pilot.py"

permissions:
  contents: read

jobs:
  pilot:
    name: Bounded adjusted Yahoo market pilot
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync --all-groups --frozen
      - name: Lint research pilot
        run: uv run ruff check scripts/research_market_yahoo_pilot.py
      - name: Fetch bounded adjusted 2013-2022 pilot
        run: >-
          uv run python scripts/research_market_yahoo_pilot.py
          --start 2013-01-01
          --end 2022-12-30
          --output work/yahoo-market-pilot
      - name: Assert research boundary and plumbing gate
        shell: bash
        run: |
          set -euo pipefail
          uv run python - <<'PY'
          import json
          from pathlib import Path
          s = json.loads(Path("work/yahoo-market-pilot/summary.json").read_text())
          assert s["status"] == "YAHOO_ACTIVE_ADJUSTED_PILOT_PASS"
          assert s["serverSideDateBoundsRequested"] is True
          assert s["providerNeutralCsvReload"] is True
          assert s["spyCovered"] is True
          assert s["coverageRate"] >= 0.90
          assert s["adjustedPricesAvailable"] is True
          assert s["delistedCoverageValidated"] is False
          assert s["historicalIdentityValidated"] is False
          assert s["marketDataJoined"] is False
          assert s["canonicalReady"] is False
          assert s["signalReady"] is False
          assert s["oosOpened"] is False
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: yahoo-market-pilot-2013-2022-${{ github.run_id }}
          path: work/yahoo-market-pilot
          retention-days: 90
