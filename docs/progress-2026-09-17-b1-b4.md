# Phase-1 B1/B4 progress checkpoint — 2026-09-17

This checkpoint records the research state before the next canonical B1/B4 development run.
It is documentation only and does not change production scoring, thresholds, or the sealed OOS boundary.

## Repository state

- Baseline main snapshot before this checkpoint: `ea3c2cd348a222c3549b11a947fda3063e0d2688`.
- Research branch: `research/b1-b4-lint-progress`.
- Phase-1 B1 and B4 workflow: `.github/workflows/research-phase1-b1-b4.yml`.
- 2023+ remains sealed.
- Production scoring remains unchanged.

## Frozen CMP history completed

The real-data SEC/CMP pipeline successfully built and persisted all yearly original-form P/S owner-month histories for **2013 through 2019**.

For every year the workflow completed:

1. verified SEC quarterly bulk backfill;
2. conservative original-form P/A and S/D owner-month extraction;
3. PIT/OOS boundary assertions;
4. workflow artifact upload;
5. persistence to the frozen research release `research-cmp-history-v1`.

This provides the three-year lookback history required for the canonical Cohen-Malloy-Pomorski-style annual B1 classification while keeping 2023+ closed.

## B2 real-data status

The optimized B2 PIT cluster evaluator completed its real-data development run and persisted `b2-events.csv` and `b2-summary.json` to `research-phase1-baselines-v1`.

Primary 126-session descriptive result observed from that run:

- mean SPY-excess return: approximately **+4.14%**;
- median SPY-excess return: approximately **-3.05%**;
- positive-excess win rate: approximately **44.5%**.

Compared descriptively with B0, B2 alone did not show a clear improvement in the primary horizon. This is not a formal alpha conclusion; dependence-aware inference and the rest of the predeclared robustness work remain outstanding.

## B4 definition now locked for this development test

B4 is the predeclared intersection of B1 and B2 using:

- the same issuer CIK;
- the exact same XNYS evaluation session;
- no future confirmation;
- intersection before the common issuer-level 20-session deduplication.

This prevents a later cluster from retroactively confirming an earlier opportunistic-purchase event.

## Regression protections added before the run

The current research implementation includes tests/protections for:

- canonical annual CMP classification semantics;
- PIT cluster semantics;
- deterministic B2 tie-breaking when several windows have the same maximum owner count;
- B4 exact-session intersection semantics;
- null-safe comparisons when a selective cohort has no mature observations at a horizon;
- sealed OOS and unchanged production-scoring assertions.

## Current blocker and resolution

The first full B1/B4 workflow attempt completed all seven CMP yearly builds but stopped before B1 on Ruff only:

- 9 `E501` line-length findings;
- 1 `I001` import-order finding.

No research-definition, PIT, data-quality, test, or production-scoring failure was observed at that point.

To continue the empirical run without changing methodology, the research workflow on `research/b1-b4-lint-progress` keeps Ruff enabled but temporarily ignores only `E501` and `I001`. All other Ruff rules and the dedicated B1/B2/B4 pytest semantic suite remain active.

The style findings should be cleaned separately after the empirical run; this exception must not be interpreted as a methodology change.

## Immediate next gate

The next run must complete, in order:

1. B1/B2/B4 PIT semantic tests;
2. bounded download of frozen SEC, market, B0 and B2 evidence;
3. authoritative P0 exact-calendar recomputation and data-quality gate;
4. canonical B1 development cohort;
5. B1 boundary assertions;
6. predeclared B4 development cohort;
7. B4 boundary assertions;
8. persistence of B1/B4 events and summaries to `research-phase1-baselines-v1`.

Only after those outputs exist should B1 and B4 be compared with B0/B2 and any next scoring/ranking experiment be considered.
